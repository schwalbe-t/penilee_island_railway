
package schwalbe.penilee

import schwalbe.penilee.engine.gfx.Camera
import org.joml.*

class Player(val position: Vector3f) {

    val camera = Camera(position)

    fun update(deltaTime: Float) {
        
    }

}